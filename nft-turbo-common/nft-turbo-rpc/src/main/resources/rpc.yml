dubbo:
  consumer:
    timeout: 3000
    check: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://192.168.0.1:8848 #首次启动前务必修改成你自己的
    parameters:
      namespace: dca38c77-bef4-40e0-97c3-7779f508b899  # 自己到nacos上创建一个给dubbo用的namespce，然后和这里保持一致，首次启动前务必修改成你自己的
      group: dubbo #首次启动前务必修改成你自己的
  application:
    name: ${spring.application.name}
    qos-enable: true
    qos-accept-foreign-ip: false